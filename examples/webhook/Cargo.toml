[package]
name    = "mybot"
version = "0.1.0"
edition = "2021"

[dependencies]
ferobot = { path = "../../ferobot",  = "0.1.4", features = ["webhook"] }
tokio   = { version = "1",     features = ["full"] }
dotenvy = "0.15"
